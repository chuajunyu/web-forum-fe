import React from 'react';
import SignUp from '../../components/SignUp';

const Page: React.FC = () => {
    return (
        <div>
            <SignUp />
        </div>
    );
};

export default Page;
